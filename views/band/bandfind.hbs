{{#each bandHbs}}
    <div class="band">
        <h2>{{bandName}}</h2>
        <p>{{genres}}</p><br>
        <p>creator: {{admin.firstName}}</p>
        <a href="/band-detail/{{_id}}"><button type="submit">Find out more about us!</button></a>
    </div>
{{/each}}
